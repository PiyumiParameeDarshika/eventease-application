<div class="event-card">
    <h2>@EventName</h2>
    <p>Date: @EventDate.ToString("MMMM dd, yyyy")</p>
    <p>Location: @EventLocation</p>

    <input @bind="EventName" placeholder="Event Name" />
    <span style="color: red;" hidden="@IsValidEventName">Invalid Event Name</span>

    <input @bind="EventDate" type="date" />
    <input @bind="EventLocation" placeholder="Event Location" />
</div>

@code {
    [Parameter]
    public string EventName { get; set; }

    [Parameter]
    public DateTime EventDate { get; set; }

    [Parameter]
    public string EventLocation { get; set; }

    private bool IsValidEventName => !string.IsNullOrWhiteSpace(EventName);
}
